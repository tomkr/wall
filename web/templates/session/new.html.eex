<div class="login-form">
  <p>Sign in using your Google Apps account by using the button below.</p>
  <%= form_tag session_path(@conn, :create) %>
    <button>
      <i class="octicon octicon-sign-in"></i>
      Sign in with Google
    </button>
  </form>
  <p><strong>Note:</strong> access is restricted to Google accounts on the <code><%= Application.get_env(:wall, :oauth)[:domain] %></code> domain.</p>
</div>
